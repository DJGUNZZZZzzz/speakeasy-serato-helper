{
  "name": "speakeasy-serato-helper",
  "version": "0.2.0",
  "private": true,
  "description": "Watches local Serato history and pushes the current track to THE CRATE (speakeasycoding.com). Read-only, zero runtime dependencies.",
  "bin": "index.js",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "pkg . --targets node18-win-x64,node18-macos-x64 --out-path dist"
  },
  "dependencies": {}
}
